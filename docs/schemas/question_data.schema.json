{
  "$defs": {
    "KnowledgeData": {
      "additionalProperties": false,
      "properties": {
        "type": {
          "description": "Type of knowledge artifact produced by the question. Must be one of: 'adr' or 'classification' if included.",
          "examples": [
            "adr",
            "classification"
          ],
          "title": "Type",
          "type": "string"
        },
        "path": {
          "description": "Path to the knowledge artifact.",
          "examples": [
            "knowledge/adrs/authority.md"
          ],
          "title": "Path",
          "type": "string"
        }
      },
      "required": [
        "type",
        "path"
      ],
      "title": "KnowledgeData",
      "type": "object"
    },
    "ReasoningData": {
      "additionalProperties": false,
      "properties": {
        "status": {
          "description": "Current status of the engineering reasoning. Must be one of: 'in_progress' or 'complete'.",
          "examples": [
            "in_progress",
            "complete"
          ],
          "title": "Status",
          "type": "string"
        },
        "worksheet_path": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Path to the worksheet containing the reasoning for this question. May be omitted when no worksheet exists.",
          "examples": [
            "reasoning/worksheets/authority.md"
          ],
          "title": "Worksheet Path"
        }
      },
      "required": [
        "status"
      ],
      "title": "ReasoningData",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "properties": {
    "question": {
      "description": "The question being investigated.",
      "examples": [
        "What makes engineering information authoritative?"
      ],
      "title": "Question",
      "type": "string"
    },
    "summary": {
      "description": "A concise summary of the current understanding of the question.",
      "examples": [
        "Defines the criteria used to determine whether engineering information is authoritative."
      ],
      "title": "Summary",
      "type": "string"
    },
    "reasoning": {
      "$ref": "#/$defs/ReasoningData",
      "description": "Information describing the reasoning process for this question."
    },
    "knowledge": {
      "anyOf": [
        {
          "$ref": "#/$defs/KnowledgeData"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The knowledge artifact produced by the question, if one exists."
    },
    "prerequisites": {
      "description": "IDs of questions whose knowledge is required before this question can be resolved.",
      "examples": [
        [
          "engineering_information_types",
          "authority"
        ]
      ],
      "items": {
        "type": "string"
      },
      "title": "Prerequisites",
      "type": "array"
    }
  },
  "required": [
    "question",
    "summary",
    "reasoning",
    "prerequisites"
  ],
  "title": "QuestionData",
  "type": "object"
}
