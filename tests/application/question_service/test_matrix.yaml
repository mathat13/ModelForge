QuestionService.load:
  valid:
    - dictionary containing non-empty questions mapping
  invalid:
    - non-dictionary
    - dictionary without a non-empty questions mapping
  integration:
    - 
